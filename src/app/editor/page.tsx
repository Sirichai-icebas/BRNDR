"use client";

export default function EditorPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-[var(--color-surface)]">
      <p className="text-muted-foreground text-sm">
        Editor — coming in task6
      </p>
    </main>
  );
}
